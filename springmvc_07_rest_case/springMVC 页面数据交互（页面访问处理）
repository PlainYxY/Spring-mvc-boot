1、编写新的SpringMvcSupport配置类，目的是：设置对静态资源的访问放行
2、在 SpringMvcConfig配置类 中，用@ComponentScan({"com.yxy.controller","com.yxy.config"})扫描上 SpringMvcSupport配置类
3、启动 tomcat插件服务器，访问 http://localhost/pages/books.html

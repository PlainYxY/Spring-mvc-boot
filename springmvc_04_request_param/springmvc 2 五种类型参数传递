在Controller包下的控制类中

1、普通参数
2、普通参数：请求参数名与形参名不同
3、POJO参数
4、嵌套POJO参数（里面有引用类型(对象)）
5、数组参数
6、集合参数（与数组不同的是需要额外在形参前写 @RequestParam（注解在形参前））
注：对于 集合参数传递需要@RequestParam注解，而数组参数参数传递不需要，是因为集合的泛型在传递中会被擦除，导致不会去URL参数上去找
    所以这里 @RequestParam注解的作用是：保持一致性，解决类型擦除

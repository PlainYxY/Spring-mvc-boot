在resources目录下
放在config目录下的配置文件（application.yml）比直接放在resources下的文件优先级要高
比如两个配置文件同时存在  放config下的配置文件用81端口启动，直接放在resources下的配置文件用80启动，最后服务器用的是81

这个可以用在开发环境和正式环境使用，直接放的用作开发用，放config目录下的用作正式环境


当项目打包好后，此时在jar包 同级目录下也可以继续放application.yml配置文件和放config文件夹下的配置文件
后者的优先级依然比前者高
这个办法用作上线后使用

详细见下图所示